import {db} from "@/lib/db";
export default async function Suppliers(){const rows=await db.supplier.findMany({include:{_count:{select:{purchases:true}}},orderBy:{companyName:"asc"}});return <div className="space-y-6"><h1 className="text-2xl font-black">Suppliers</h1><div className="card p-5 overflow-x-auto"><table className="table"><thead><tr><th>Company</th><th>Contact</th><th>Phone</th><th>Purchases</th></tr></thead><tbody>{rows.map(s=><tr key={s.id}><td>{s.companyName}</td><td>{s.contactName||"-"}</td><td>{s.phone||"-"}</td><td>{s._count.purchases}</td></tr>)}</tbody></table></div></div>}
